spring:
  application:
      name: SpringbootStarterTemplate
  profiles:
    active: ${PROFILES_ACTIVE}        # active profile is "dev" switching to prod needs to handle prod.yml first


management:
  info:
    env:
      enabled: true

info:
  app:
    name: Spring Boot Starter Template
    description: Production-ready starter template
    version: 1.0.0
